const lineR = {}; // Assuming lineR is defined somewhere in your code

const myObject = {
    'line-radial': lineR,
    // You can add more properties here as needed
};

console.log(myObject);
